#ifndef EPOLLCONFIG_HPP
#define EPOLLCONFIG_HPP

#include <cstddef>

const int TIMEOUT = -1;
const bool REUSE_ADDRESS = 1;
const int LIMIT = 64;
const size_t RECV_SIZE = 4096;
const int NUM_BASE = 10;

#endif